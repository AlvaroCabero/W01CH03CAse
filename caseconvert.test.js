function caseConvert(texto) {
  return Object.is(a, b);
}

describe("funtion caseConvert", () => {
  test("When it receives texto, it returns texto fo", () => {
    //Arrange
    const firstValue = 1;
    const secondValue = 1;
    const expected = true;

    //Act
    const result = strictEquals(firstValue, secondValue);

    //Asset
    expect(result).toBe(expected);
  });
});
